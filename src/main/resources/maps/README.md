# Reserved Map Folder

This folder is reserved for game map templates that are copied into the plugin data directory at runtime.

Expected runtime location:

- `plugins/KaGameCenter/maps/`

Game-specific templates should be placed under subfolders such as:

- `plugins/KaGameCenter/maps/parkour/default/`
